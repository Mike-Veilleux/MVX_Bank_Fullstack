export enum ETransactionType {
  DEPOSIT = "Deposit",
  WITHDRAW = "Withdraw",
  TRANSFER = "Transfer",
}

export enum IAccountType {
  SAVINGS = "SAVINGS",
  CHECK = "CHECK",
}

export enum IUserType {
  NONE = 0,
  LOCAL = 1,
  GOOGLE = 2,
}
